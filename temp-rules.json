{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth.uid === $uid",
        ".write": "auth.uid === $uid",
        "chatSessions": {
          ".indexOn": ["startedAt", "status", "lastMessageAt", "timestamp"]
        }
      }
    }
  }
}
